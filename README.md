Implements insertion in an AVL tree, using the following node structure:

       Node { 

                  int key,
                  int height,
                  Node leftchild,
                  Node RightChild,
                  Node parent,
             };
